
import { ParticleRendererProps } from "../types";
import { renderBaseParticle } from "./base-renderer";

export function renderDefaultParticle(props: ParticleRendererProps): void {
  renderBaseParticle(props);
}
